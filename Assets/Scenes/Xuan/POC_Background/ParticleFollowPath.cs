using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleFollowPath : MonoBehaviour {

    public string pathname;
    public float time;
	// Use this for initialization
	void Start () {
        //iTween.MoveTo(gameObject,iTween.Hash("path", iTweenPath.GetPath(pathname), "looptype","loop", "easetype", iTween.EaseType.easeInSine,"time",time));
	}
	

}
